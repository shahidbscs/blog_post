<h1>Edit Blog</h1>
  <%= form_for :blog, url: blogs_url do |f| %>
    <p>
			<%= f.label :title %>
			<%= f.text_field :title %>
		</p>
	  <p>
			<%= f.label :description %>
			<%= f.text_area :description %>
		</p>
	  <p>
			<%= f.submit :submit %>
		</p>
  <% end %>